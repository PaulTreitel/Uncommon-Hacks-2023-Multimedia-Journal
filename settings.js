import * as React from 'react';
import { StyleSheet, Text, View, Button } from 'react-native';

export const SettingsScreen = ({navigation, route}) => {
    return (
        <Text>This is you settings page. Right now there's nothing here.</Text>
    
        );
  };